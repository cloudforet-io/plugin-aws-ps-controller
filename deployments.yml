apiVersion: apps/v1
kind: Deployment
metadata:
  name: aws-power-controller
  labels:
    domain_id: domain-8b5c2f2f9ca1
    plugin_name: aws-power-controller
    service_type: power_scheduler.Controller
    supervisor_name: root
    version: '441ce36-dirty'
  namespace: spaceone
spec:
  replicas: 1
  selector:
    matchLabels:
      domain_id: domain-8b5c2f2f9ca1
      plugin_name: aws-power-controller
      service_type: power_scheduler.Controller
      supervisor_name: root
      version: '441ce36-dirty'
  template:
    metadata:
      labels:
        domain_id: domain-8b5c2f2f9ca1
        plugin_name: aws-power-controller
        service_type: power_scheduler.Controller
        supervisor_name: root
        version: '441ce36-dirty'
    spec:
      containers:
      - name: aws-power-controller
        image: 110365492710.dkr.ecr.ap-northeast-2.amazonaws.com/spaceone/aws-power-controller:latest
        ports:
        - containerPort: 50051
          protocol: TCP
        imagePullPolicy: Always
